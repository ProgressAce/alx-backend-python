#!/usr/bin/env python3
"""Defines function that returns the sum of two floating point numbers."""


def add(a: float, b: float) -> float:
    """Sums two floating point numbers.

    Args:
        a(float): number 1.
        b(float): number 2.
    Returns:
        sum of <a> and <b>."""

    return a + b
